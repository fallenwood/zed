[build]
# v0 mangling scheme provides more detailed backtraces around closures
rustflags = ["-C", "symbol-mangling-version=v0", "-C", "link-arg=-fuse-ld=lld", "--cfg", "tokio_unstable"]

[alias]
xtask = "run --package xtask --"
